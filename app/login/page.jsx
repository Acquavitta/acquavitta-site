'use client'; import {useState} from 'react'; import Link from 'next/link'; import Brand from '../../components/Brand';
export default function Login(){const [msg,setMsg]=useState(''); async function submit(e){e.preventDefault(); const f=new FormData(e.currentTarget); setMsg('Secure login interface ready. Backend authentication will be connected in the next stage.');} return <main className="auth"><div className="auth-card"><Brand/><span className="eyebrow">MEMBER ACCESS</span><h1>Sign in</h1><form onSubmit={submit}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="gold">Sign in securely</button></form>{msg&&<div className="message">{msg}</div>}<p className="small">Need access? <Link href="/register">Create an account</Link></p></div></main>}
